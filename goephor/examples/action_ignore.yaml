  name: "Project Name"
  description: "Project Description"
  globals: 
    - BASE_PATH: "/tmp"
    - WORK: "${BASE_PATH}/ADDIN"
    - var1: "1"
    - var2: "2"
  actions:
       - system.terminal.shell:
                            - "echo 'Action 1'"
       # This will fail
       - system.terminal.shell:
                            - "echo 'Action 2 failure' && echo some'"
                            - "echo 'Action 2 ignore is used to run on a failure event'"
                            - "echo 'Action 2a'"
                            - test: "True"
                            - WALDO: "False"
       # This will run because ignore is True
       - system.terminal.shell:
                            - "echo 'Action 3 ignore runs even if failure occurs'"
                            - ignore: True
       # This will not run
       - system.terminal.shell:
                            - "echo 'Action 4 This will not run as ignore is not used'"