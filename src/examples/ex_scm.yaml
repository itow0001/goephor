  name: "Project Name"
  description: "Project Description"
  globals: 
    - REPO: "/tmp/freebsd"
    - GIT_REMOTE: "git@github.west.isilon.com:sdouglas2/freebsd.git"
  actions:
       # make sure the user has a key
       - system.terminal.shell:
                            - "whoami"
       # root user needs the correct key
       - system.terminal.shell:
                              - "cp /jenkins/.ssh/id_rsa /root/.ssh" 
       # This is how to clone a repo                       
       - scm.git.clone:
                      - "root"
                      - "${REPO}"
                      - "${GIT_REMOTE}"
                      - branch: "stable/10"
       - system.terminal.shell:
                              - "ls ${REPO}"
       # checkout a branch
       #- scm.git.checkout:
       #   - "root"
       #   - "${REPO}"
       #   - "origin/stable/10"
       #- scm.git.delete:
       #               - "${REPO}"
                      
